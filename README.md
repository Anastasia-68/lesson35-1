# Webpack Advanced Configuration Project

Учебный проект, демонстрирующий продвинутую конфигурацию Webpack для современной веб-разработки. Проект включает в себя работу с активами, препроцессорами, типизацией и инструментами анализа сборки.

## 🛠 Реализованные возможности

В соответствии с техническим заданием в проект интегрированы следующие функции:

### Базовая конфигурация и Ассеты
* **Хеширование имен**: Все выходные файлы имеют уникальные хеши (`[contenthash]`) для предотвращения проблем с кэшированием в браузере.
* **Локальные шрифты**: Настроена поддержка шрифтов (Roboto) через `@font-face` и Asset Modules.
* **Обработка изображений**: Оптимизированная загрузка изображений.
* **Внешние CSS**: Поддержка подключения стилей через отдельные файлы.

### Продвинутые технологии
* **TypeScript**: Полная поддержка компиляции TS-файлов через `ts-loader`.
* **Sass/SCSS**: Использование препроцессоров для написания гибких стилей.
* **Babel**: Транспиляция кода для обеспечения совместимости с современными стандартами JavaScript.
* **ESLint**: Интеграция статического анализатора кода для поддержания чистоты проекта.

### Инструменты разработки и оптимизации
* **DevServer**: Настроена автоматическая перезагрузка страницы (Hot Module Replacement) при изменениях в коде.
* **Bundle Analyzer**: Визуализация состава и размера сборки (отчет генерируется в `dist/report.html`).
* **Оптимизация**: Настройка разделения кода (Code Splitting) для повышения производительности.

## Как запустить проект

1. **Установка зависимостей**:
   ```bash
   npm install